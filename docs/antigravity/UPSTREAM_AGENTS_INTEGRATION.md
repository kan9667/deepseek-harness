# Preserve DeepSeek Harness AGENTS.md

Do not create an SEH replacement for the root `AGENTS.md`. DSH uses it for repository layout, pre-release compatibility posture, commands, tests, Agent Notes, ESM, Cordis, documentation, snapshots, SDKs, vendoring, and defensive patterns.

This pack adds `GEMINI.md` and workspace customizations. Antigravity must read both SEH and DSH instructions. When working below a package, read the nearest `AGENTS.md` as well.

If upstream renames, moves, or changes instruction files, treat that as an upstream-sync compatibility item.
