---
name: seh-upstream-sync
description: Evaluates a newer DeepSeek Harness commit for compatibility without silently changing the approved SEH baseline.
---

# Upstream sync

Use an isolated branch. Compare repository instructions, package/toolchain, capability and config catalogs, LLM middleware, sessions, persistence, tools/approvals, sandbox, credentials/settings, profiles/bundles, UI, and model-visible outputs. Run SEH compatibility plus upstream gates. Produce accept/defer report and move pin only with approval.
