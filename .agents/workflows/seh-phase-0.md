---
description: Execute a contracts-only Phase 0 implementation slice with no live model, steering, remote, or training behavior.
---
# SEH Phase 0

1. Run `/seh-session-start`; confirm active phase `0` and foundation-only feature flags.
2. Define the narrow contract, actor/authority map, evidence owner, public compatibility, and negative tests before code.
3. Reuse DSH/Cordis seams. Add only browser-safe contracts, schemas, IDs, actor/protected-target data, empty service interfaces, fake fixtures, and tests.
4. For steering/training, define contracts and denial tests only. Do not extract/load a vector, select non-neutral alpha, install Unsloth, or start a run.
5. Prove optimizer/trainer cannot qualify, gate, activate, release, access hidden sets, or bypass the GPU scheduler.
6. Run focused tests and affected DSH documentation/build gates.
7. Confirm every live feature remains disabled and record the next dependency-ordered slice.
